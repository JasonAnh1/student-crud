
entity Student {
	Name String,
    Code String,
    Phone String,
    Birthday LocalDate,
    Address String
}

paginate Student with pagination

// Use Data Transfer Objects (DTO)
// dto * with mapstruct

// Set service options to all except few
service all with serviceImpl except Student

// Set an angular suffix
// angularSuffix * with mySuffix
